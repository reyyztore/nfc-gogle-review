export const metadata = {
  title: 'Reyyztore NFC Review',
  description: 'NFC & QR Code Review Platform',
}

export default function RootLayout({ children }) {
  return (
    <html lang="id">
      <body>{children}</body>
    </html>
  )
}
